/*
'use strict';

before(function() {
    var express = require('express'),
        config = require('../usageReporting/');
    var agent = require('supertest');


    var app = express();

   // require('./express')(app, config);
/!*
    app.listen(9507, function() {
        console.log('Express server listening on port ' + 9507);
    });*!/

    console.log('before');
    global.app = app;
    global.agent = agent(app);
});*/
